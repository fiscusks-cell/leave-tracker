import React from "react";
import ReactDOM from "react-dom/client";
import "./storage.js";
import LeaveTracker from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LeaveTracker />
  </React.StrictMode>
);
